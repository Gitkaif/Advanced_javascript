// function greet() {
//   console.log(this.name);
// }

// const user = { name: "Kaif" };

// greet.call(user);   // forcing user to be this of greet

//------------------------------------------------------------------------------------------------------------------------------------------------

// const user = {
//     name:"kaif",
//     greet(){
//         console.log(this.name);

//     }
// }

// const fn = user.greet;
// fn()   // undefined
// fn.call(user)  // force this to user

//------------------------------------------------------------------------------------------------------------------------------------------------

function greet(city, state) {
  console.log(this.name, city, state);
}

const user = { name: "kaif" };
greet.call(user,'mumbai','maharashtra')   // we can use call with multiple arguments 

//------------------------------------------------------------------------------------------------------------------------------------------------

//------------------------------------------------------------------------------------------------------------------------------------------------

//------------------------------------------------------------------------------------------------------------------------------------------------
